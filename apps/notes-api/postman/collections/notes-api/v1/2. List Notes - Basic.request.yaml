$kind: http-request
name: '2. List Notes - Basic'
method: GET
url: '{{baseUrl}}/notes?first=10'
queryParams:
  - key: first
    value: '10'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const body = pm.response.json();
      if (body && body.pageInfo) {
          pm.environment.set('endCursor', body.pageInfo.endCursor);
          pm.environment.set('startCursor', body.pageInfo.startCursor);
      }

      pm.test('List Notes - Basic responds with 200', function () {
          pm.expect(pm.response.code).to.be.oneOf([200]);
      });
examples: ./.resources/2. List Notes - Basic.resources/examples
order: 2000

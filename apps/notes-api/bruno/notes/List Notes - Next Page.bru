meta {
  name: List Notes - Next Page
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/notes?first=10&after={{endCursor}}
  body: none
  auth: none
}

params:query {
  first: 10
  after: {{endCursor}}
}

docs {
  Run "List Notes - First Page" first — it stores `meta.endCursor` into
  the {{endCursor}} variable this request pages forward from.
}

script:post-response {
  if (res.body && res.body.meta) {
    bru.setVar("endCursor", res.body.meta.endCursor);
    bru.setVar("startCursor", res.body.meta.startCursor);
  }
}

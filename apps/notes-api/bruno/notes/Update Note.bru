meta {
  name: Update Note
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/notes/{{noteId}}
  body: json
  auth: none
}

body:json {
  {
    "title": "My first note (edited)",
    "tags": ["personal", "edited"]
  }
}
